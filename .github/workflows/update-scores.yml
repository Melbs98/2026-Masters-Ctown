name: Historical Static Site

on:
  workflow_dispatch:

jobs:
  static-site-only:
    runs-on: ubuntu-latest
    steps:
      - name: Historical site only
        run: echo "This repo is archived as a static historical site. No score updates will run."
